name: Build Auction Service Latest

on:
  pull_request:
  push:
    branches:
      - main
    paths:
      - 'src/Auctions/*'

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
           
      - name: Login to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Build and Push :latest Image
        uses: docker/build-push-action@v5
        with:
          context: src/Auctions
          platforms: linux/arm64
          push: true
          tags: |
            ghcr.io/seancallaway/carbay/auction-service:latest
            ghcr.io/seancallaway/carbay/auction-service:${{ github.sha }}

